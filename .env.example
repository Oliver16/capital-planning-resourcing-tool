# Copy this file to .env.local (or .env.development) and supply your Supabase credentials.
# The values below are placeholders.

# Create React App exposes variables prefixed with REACT_APP_. If you're running the
# Vercel + Supabase integration locally you can also export STORAGE_NEXT_PUBLIC_* values –
# the tooling will normalize either set of names.

REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key
# STORAGE_NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# STORAGE_NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
